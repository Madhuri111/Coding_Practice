#include<bits/stdc++.h>
using namespace std;

int main()
{
	vector<int> A;
	A.push_back(10);
	A.push_back(20);
	A.push_back(30);
	A.push_back(50);
	//at gives value of array at that index
	cout << A.at(2) << endl;
	//front gives value of first element
	cout << A.front() << "front" << endl;
	//back gives value of last element
	cout << A.back() << "back" << endl;
	//data() – Returns a direct pointer to the memory array used internally by the vector to store its owned elements.

	    int* pos = g1.data();

}

